dataset:
  name: "UIUC"
  reader_type: "UIUC"
  path: "datasets/UIUC/UIUC_pt"
  dataset_setup:
    type: "cross-validation"
    folds_splitter: "shuffle-split"
    folds: 5
    test_size: 0.4
    random_state: 0
  filter:
    answer_type: 'OTHER'